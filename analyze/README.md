#### 近似値評価

```markdown
# approximate.py

② 近似値評価、説明文を出力先に追記した拡張版。

# if_approximate_mix.py

③ 近似値評価(float型)、if文トレーニングデータとテストデータ両方で条件分岐。

# if_approximate_train.py

④近似値評価(float型)、if文トレーニングデータで条件分岐。

# if_approximate_test.py

⑤ 近似値評価(float型)、if文テストデータで条件分岐。

# pca_approximate.py

① PCA(主成分分析)を利用した近似値評価、標準機能。
```

_※ 各ファイルの説明文。_
